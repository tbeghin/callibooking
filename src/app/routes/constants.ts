import { SHOW_LIST_TITLE, SHOW_LIST_URL } from 'Pages/ShowList/constants';
import { PIECE_MANAGE_TITLE, PIECE_MANAGE_URL } from 'Pages/PieceManage/constants';

const ROUTE_URL = {
  SHOW_LIST_URL,
  PIECE_MANAGE_URL,
};

export const Constants = {
  SHOW_LIST_TITLE,
  PIECE_MANAGE_TITLE,
};

export default ROUTE_URL;
